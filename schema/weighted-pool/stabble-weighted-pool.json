{
  "contract_name": "stabble-weighted-pool",
  "contract_version": "0.1.0",
  "idl_version": "1.0.0",
  "instantiate": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "InstantiateMsg",
    "type": "object",
    "additionalProperties": false
  },
  "execute": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "ExecuteMsg",
    "oneOf": [
      {
        "type": "object",
        "required": ["initialize"],
        "properties": {
          "initialize": {
            "type": "object",
            "required": ["pool_mint", "pool_mode", "swap_fee", "token_accounts", "vault_address", "weights"],
            "properties": {
              "pool_mint": {
                "$ref": "#/definitions/TokenMsg"
              },
              "pool_mode": {
                "$ref": "#/definitions/PoolMode"
              },
              "swap_fee": {
                "type": "integer",
                "format": "uint16",
                "minimum": 0.0
              },
              "token_accounts": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Addr"
                }
              },
              "vault_address": {
                "$ref": "#/definitions/Addr"
              },
              "weights": {
                "type": "array",
                "items": {
                  "type": "integer",
                  "format": "uint16",
                  "minimum": 0.0
                }
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["close"],
        "properties": {
          "close": {
            "type": "object",
            "required": ["pool_id"],
            "properties": {
              "pool_id": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["add_liquidity"],
        "properties": {
          "add_liquidity": {
            "type": "object",
            "required": ["min_out_amount", "pool_id", "tokens", "vault_address"],
            "properties": {
              "min_out_amount": {
                "$ref": "#/definitions/Uint128"
              },
              "pool_id": {
                "type": "string"
              },
              "tokens": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/TokenMsg"
                }
              },
              "vault_address": {
                "$ref": "#/definitions/Addr"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["remove_liquidity"],
        "properties": {
          "remove_liquidity": {
            "type": "object",
            "required": ["amount", "min_out_amounts", "pool_id", "tokens", "vault"],
            "properties": {
              "amount": {
                "$ref": "#/definitions/Uint128"
              },
              "min_out_amounts": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Uint128"
                }
              },
              "pool_id": {
                "type": "string"
              },
              "tokens": {
                "type": "array",
                "items": {
                  "$ref": "#/definitions/Addr"
                }
              },
              "vault": {
                "$ref": "#/definitions/Addr"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["swap"],
        "properties": {
          "swap": {
            "type": "object",
            "required": ["min_amount", "pool_id", "token_a", "token_b", "treasury_account", "vault"],
            "properties": {
              "min_amount": {
                "$ref": "#/definitions/Uint128"
              },
              "pool_id": {
                "type": "string"
              },
              "token_a": {
                "$ref": "#/definitions/TokenMsg"
              },
              "token_b": {
                "$ref": "#/definitions/Addr"
              },
              "treasury_account": {
                "$ref": "#/definitions/Addr"
              },
              "vault": {
                "$ref": "#/definitions/Addr"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ],
    "definitions": {
      "Addr": {
        "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
        "type": "string"
      },
      "PoolMode": {
        "type": "string",
        "enum": ["managed", "normal"]
      },
      "TokenMsg": {
        "type": "object",
        "required": ["amount", "token_address"],
        "properties": {
          "amount": {
            "$ref": "#/definitions/Uint128"
          },
          "token_address": {
            "$ref": "#/definitions/Addr"
          }
        },
        "additionalProperties": false
      },
      "Uint128": {
        "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
        "type": "string"
      }
    }
  },
  "query": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "QueryMsg",
    "oneOf": [
      {
        "description": "Show all pools. Return type is ListResponse.",
        "type": "object",
        "required": ["list"],
        "properties": {
          "list": {
            "type": "object",
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      },
      {
        "description": "Returns the details of the named pool, error if not created Return type: DetailsResponse.",
        "type": "object",
        "required": ["details"],
        "properties": {
          "details": {
            "type": "object",
            "required": ["id"],
            "properties": {
              "id": {
                "type": "string"
              }
            },
            "additionalProperties": false
          }
        },
        "additionalProperties": false
      }
    ]
  },
  "migrate": null,
  "sudo": null,
  "responses": {
    "details": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Pool",
      "type": "object",
      "required": ["admin", "invariant", "is_active", "pool_mint", "pool_mode", "swap_fee", "tokens", "vault"],
      "properties": {
        "admin": {
          "$ref": "#/definitions/Addr"
        },
        "invariant": {
          "$ref": "#/definitions/Uint128"
        },
        "is_active": {
          "type": "boolean"
        },
        "pool_mint": {
          "$ref": "#/definitions/Addr"
        },
        "pool_mode": {
          "$ref": "#/definitions/PoolMode"
        },
        "swap_fee": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0.0
        },
        "tokens": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PoolToken"
          }
        },
        "vault": {
          "$ref": "#/definitions/Addr"
        }
      },
      "additionalProperties": false,
      "definitions": {
        "Addr": {
          "description": "A human readable address.\n\nIn Cosmos, this is typically bech32 encoded. But for multi-chain smart contracts no assumptions should be made other than being UTF-8 encoded and of reasonable length.\n\nThis type represents a validated address. It can be created in the following ways 1. Use `Addr::unchecked(input)` 2. Use `let checked: Addr = deps.api.addr_validate(input)?` 3. Use `let checked: Addr = deps.api.addr_humanize(canonical_addr)?` 4. Deserialize from JSON. This must only be done from JSON that was validated before such as a contract's state. `Addr` must not be used in messages sent by the user because this would result in unvalidated instances.\n\nThis type is immutable. If you really need to mutate it (Really? Are you sure?), create a mutable copy using `let mut mutable = Addr::to_string()` and operate on that `String` instance.",
          "type": "string"
        },
        "PoolMode": {
          "type": "string",
          "enum": ["managed", "normal"]
        },
        "PoolToken": {
          "type": "object",
          "required": ["balance", "decimals", "mint", "multiplier", "normalized_weight", "scaling_factor"],
          "properties": {
            "balance": {
              "$ref": "#/definitions/Uint128"
            },
            "decimals": {
              "type": "integer",
              "format": "uint8",
              "minimum": 0.0
            },
            "mint": {
              "$ref": "#/definitions/Addr"
            },
            "multiplier": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            },
            "normalized_weight": {
              "type": "integer",
              "format": "uint16",
              "minimum": 0.0
            },
            "scaling_factor": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0.0
            }
          },
          "additionalProperties": false
        },
        "Uint128": {
          "description": "A thin wrapper around u128 that is using strings for JSON encoding/decoding, such that the full u128 range can be used for clients that convert JSON numbers to floats, like JavaScript and jq.\n\n# Examples\n\nUse `from` to create instances of this and `u128` to get the value out:\n\n``` # use cosmwasm_std::Uint128; let a = Uint128::from(123u128); assert_eq!(a.u128(), 123);\n\nlet b = Uint128::from(42u64); assert_eq!(b.u128(), 42);\n\nlet c = Uint128::from(70u32); assert_eq!(c.u128(), 70); ```",
          "type": "string"
        }
      }
    },
    "list": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ListResponse",
      "type": "object",
      "required": ["pools"],
      "properties": {
        "pools": {
          "description": "list all registered ids",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  }
}
